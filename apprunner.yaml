version: 1.0
runtime: go1
build:
  commands:
    build:
      - go build -o oauth-service .
run:
  runtime-version: 1.25
  command: ./oauth-service
  network:
    port: 8080
  env:
    - name: PORT
      value: "8080"