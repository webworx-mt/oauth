version: 1.0
runtime: docker
build:
  commands:
    build:
      - echo "Building OAuth service..."
      - docker build -t oauth-service .
run:
  command: docker run -p 8080:8080 oauth-service
  network:
    port: 8080
    env: PORT
  env:
    - name: PORT
      value: "8080"
